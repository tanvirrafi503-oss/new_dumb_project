<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VaultApp">

        <!-- Splash / Lock Screen (launcher) -->
        <activity
            android:name=".ui.LockActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main vault list -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="false" />

        <!-- Add / Edit credential -->
        <activity
            android:name=".ui.EditCredentialActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Credential detail view -->
        <activity
            android:name=".ui.CredentialDetailActivity"
            android:exported="false" />

        <!-- Setup / first-run -->
        <activity
            android:name=".ui.SetupActivity"
            android:exported="false" />

        <!-- Accessibility Service for auto-capture -->
        <service
            android:name=".service.VaultAccessibilityService"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Save-prompt overlay -->
        <activity
            android:name=".ui.SavePromptActivity"
            android:exported="false"
            android:theme="@style/Theme.VaultApp.Dialog"
            android:launchMode="singleTask" />

    </application>
</manifest>
